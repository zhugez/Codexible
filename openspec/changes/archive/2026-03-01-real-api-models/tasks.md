# Tasks: Real API Models

## 1. Update Mock Data Service
- [x] 1.1 Replace hardcoded model names in `mockDashboardData.ts` with real Codexible API model names.
- [x] 1.2 Update `getModelBreakdown()` to use a representative subset of real models.

## 2. Fix API Key Format
- [x] 2.1 Update API key generation in `ApiKeyManager.tsx` to use `sk-{64_hex_chars}` format.

## 3. Verification
- [x] 3.1 Verify the dashboard displays real model names and the new key format.
