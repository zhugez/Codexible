import type { Translations } from "@/app/types";
import { vi } from "./vi";
import { en } from "./en";

export const translations: Translations = { vi, en };

export type { Lang, Translation, Translations } from "@/app/types";
